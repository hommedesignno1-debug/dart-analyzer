name: DART 자동 분석 (watchlist)

on:
  schedule:
    # 매일 아침 8시 KST = UTC 23시 전날 (월~금 = 일~목 UTC)
    # 기존 네이버 파이프라인과 동일 스케줄
    - cron: '0 23 * * 0-4'
  workflow_dispatch:  # 수동 트리거도 가능

jobs:
  batch-analyze:
    runs-on: ubuntu-latest
    timeout-minutes: 60  # 최대 1시간 (종목당 ~2분 × 20개)

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run Watchlist Analysis
        env:
          DART_API_KEY: ${{ secrets.DART_API_KEY }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          GMAIL_USER: ${{ secrets.GMAIL_USER }}
          GMAIL_APP_PASSWORD: ${{ secrets.GMAIL_APP_PASSWORD }}
        run: python main.py --watchlist

      - name: Commit reports
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add reports/
          git diff --staged --quiet || git commit -m "📊 일일 자동 분석 리포트 ($(date +'%Y-%m-%d'))"
          git push || true
